# --- Core API server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Agent orchestration ---
langgraph==0.2.34
langchain-core==0.3.6
openai==1.51.0          # used with base_url override -> talks to NVIDIA NIM (OpenAI-compatible)

# --- Persistence (SQLite by default, swap DATABASE_URL for Postgres) ---
sqlalchemy==2.0.35
aiosqlite==0.20.0

# --- Optional real-data integrations (wire in when you have keys, see .env.example) ---
amadeus==11.0.0          # flights/hotels (Amadeus for Developers, free self-service tier)
httpx==0.27.2            # generic HTTP client, used for any REST integration (Skyscanner/Serper/etc.)

# --- Budget guardrail ---
# AgentBudget (open-source "ulimit for AI agents") — install from GitHub, not yet on PyPI:
# pip install git+https://github.com/AgentBudget/agentbudget.git
# A minimal in-house fallback is implemented in app/agent/budget_guard.py so the
# project runs standalone even before you wire the real package in.

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
